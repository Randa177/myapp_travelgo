import 'package:flutter/material.dart';

class BiblioPage extends StatelessWidget {
  const BiblioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text("Hello From Biblio Page")]));
  }
}
